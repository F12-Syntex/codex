# Codex Environment Variables
# Copy this file to .env and fill in your values.

# OpenRouter API key (required for AI features: wiki, simulate, style dictionary)
OPENROUTER_API_KEY=

# Node environment — set automatically by scripts, no need to change manually.
# "development" enables dev-only features (e.g., Novel Installer).
# NODE_ENV=development
