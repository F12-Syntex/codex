# Codex Environment Variables
# Copy this file to .env to enable dev-only features.

# Set to "development" to show dev-only features (e.g., Novel Installer).
# This is set automatically by `yarn electron:dev`, but you can override it here.
NODE_ENV=development
